{% block navbar %}
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
        <div class="container">
            <a class="navbar-brand" href="index.html">Le blog de Romain</a>
            <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
                Menu
                <i class="fas fa-bars"></i>
            </button>
            <div class="collapse navbar-collapse" id="navbarResponsive">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="index.php?page=home&method=default">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="index.php?page=blogPost&method=allBlockPostMethod">BlogPosts</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="index.php?page=home&method=aboutMethod">A propos</a>
                    </li>
                    {% if number > 0 %}
                    <li class="nav-item" style="background-color: orange">
                        <a class="nav-link" href="index.php?page=comment&method=allCommentDisableMethod">A valider !</a>
                    </li>
                    {% endif %}
                    <li class="nav-item">
                        {% if session.id_user is not null %}
                            <a class="nav-link" href="index.php?page=login&method=logoutMethod">Se déconnecter</a>
                        {% else %}
                            <a class="nav-link" href="index.php?page=login&method=loginMethod">Connexion</a>
                        {% endif %}
                    </li>
                    <li class="nav-item">
                        {% if session.id_user is null %}
                            <a class="nav-link" href="index.php?page=login&method=createAccountMethod">Inscription</a>
                        {% endif %}
                    </li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}